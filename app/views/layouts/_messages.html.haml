.row
  .col-xs-10.col-xs-offset-1
    -flash.each do |name, msg|
      %div{class: "alert alert-#{name}"}
        %a{href: '#', class: "close", data_dismiss: "alert"} &#215;
        -if msg.is_a?(String)
          %div{id: "flash#{name}"}= msg