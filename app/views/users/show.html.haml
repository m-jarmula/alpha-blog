%h1.center= "Welocome #{@user.username}"
.row
  .col-md-4.col-md-offset-4.center
    =gravater_for @user, size: 200
%h4.center= "#{@user.username}'s articles'"
=render 'articles/article', collection: @user.articles